# prelim — first preliminary powered run

- **`prelim-digest.md`** — the human-readable summary. Per-cell directions for all four experiments across DeepSeek and Claude Haiku, with skeptical flags. Headline: behavioral self-access to own content is real *in-context* but appears context-bound; E1 is likely a prose-preference confound; E3 near-null. (The "context-bound" reading is then sharpened by the self-recognition follow-up, which shows the out-of-context collapse is *episodic*, not perceptual — see `../self-recognition/`.)
- **`results-prelim.json`** — raw data: every trial's classification, plus the full model-generated arguments. n≈20/cell (n=6 for the round-heavy E3). ~1,300 API calls. Benign content throughout.

Decision rule (fixed in advance): power up only where a direction shows. Outcome: E2 and E5 advanced; E1 flagged for redesign; E3 parked.
